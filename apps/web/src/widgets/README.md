# widgets

This folder is intentionally empty. It exists by project decision to keep
all six FSD layer folders present; code moves in only when the criteria
below are met.

Widgets are officially discouraged in FSD v2.1. Screen compositions live in
pages/, reused actions with UI in features/, context-free UI in shared/ui.
Populate only with a deliberate, documented decision.
