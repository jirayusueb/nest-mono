# features

This folder is intentionally empty. It exists by project decision to keep
all six FSD layer folders present; code moves in only when the criteria
below are met.

Features hold reusable user actions. Move code here only when the extraction
rule holds: 2+ real consumers, independent reason to change, focused
responsibility. Single-consumer code stays in its page.
